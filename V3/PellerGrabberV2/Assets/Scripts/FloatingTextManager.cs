using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FloatingTextManager : MonoBehaviour
{
    // public GameObject floatingTextPrefab;

    // public void ShowFloatingText(string text, Vector3 position)
    // {
    //     var textInstance = Instantiate(floatingTextPrefab, position, Quaternion.identity, this.transform);
    //     textInstance.GetComponent<TextMeshPro>().text = text; // Assurez-vous d'ajuster selon votre composant de texte
    //     textInstance.SetActive(true);
    //     Destroy(textInstance, 2.0f);
    // }
}
